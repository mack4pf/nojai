import { AdminCopyTradePageClient } from "@/components/admin/admin-copy-trade-page-client";

export const metadata = { title: "Copy-Trade — Admin" };

export default function AdminCopyTradePage() {
  return <AdminCopyTradePageClient />;
}
