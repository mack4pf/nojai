import { AdminMt5CopyTradeManager } from "@/components/admin/admin-mt5-copy-trade-manager";

export const metadata = { title: "MT5 Copy Trading — Admin" };

export default function AdminMt5CopyTradingPage() {
  return <AdminMt5CopyTradeManager />;
}
